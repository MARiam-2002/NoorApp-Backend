# Backend remaining work (Flutter perspective)

> ##### 🔒 READ-ONLY POINTER — DO NOT EDIT THIS FILE
> This file is intentionally kept as a minimal stub so **older links and bookmarks shared with the Flutter team continue to resolve without returning 404**.
>
> - **✅ Source of truth for feature backlog + broken-item tracking →** [BACKEND_REQUIREMENTS.md](./BACKEND_REQUIREMENTS.md) **(NEW: OFFICIAL — uploaded from Flutter spec 2026-08-22)**
> - **Source of truth for API wire-contracts, Dart snippets, and DO/DON'T agent rules →** [FLUTTER_INTEGRATION_GUIDE.md](./FLUTTER_INTEGRATION_GUIDE.md)
> - **Product roadmap (non-backend UI/UX priorities) →** [APP_ENHANCEMENTS.md](./APP_ENHANCEMENTS.md)

## Quick status — 2026-08-22 snapshot

(For the current, always-up-to-date version of this table + per-section audits, open the full requirements doc above.)

| Priority | Item | 2026-08-22 Status | Flutter contract |
| -------- | ---- | ----------------- | ---------------- |
| P0 | Fix `GET /dashboard` HTTP 500 for authenticated users | ✅ RESOLVED — Global error handler now maps `TokenExpiredError` → `TOKEN_EXPIRED` (refresh), `JsonWebTokenError` → `INVALID_TOKEN` (logout), Prisma/Pg errors → `DATABASE_ERROR` (retry). Dashboard service also applies an Islamic-content fallback envelope instead of bubbling 5xx on DB issues. | 🔒 Locked — do not touch envelope fields: `greeting`, `prayers`, `verseOfTheDay`, `hadithOfTheDay`, `dailyJourney`, `khatmah`, `dailyChallenge`, `utilities`. |
| P0 | Keep Quran Bismillah/BOM + dual-counter contracts stable | 🔒 LOCKED — (1) `textAr` hygiene pipeline strips the opening decorative Bismillah on ayah #1 of every surah EXCEPT 1 and 9; (2) BOM (U+FEFF) stripped from every returned `textAr`; (3) Reading counter lives in `Khatmah.totalPagesRead` + `currentPage` (aggregated into `progressPercent`, `dailyGoal`, `stats.streakDays`, `stats.completedKhatmahCount`, `stats.totalPagesRead` on `GET /quran/khatmah/stats`). The "daily-journey" Quran side counter (`DailyProgress.quranPagesRead`) is a **separate, unrelated per-day bucket** used only for daily-challenge eligibility and the dashboard's `dailyJourney.quran.pagesRead` widget — never subtract the two counters. | 🔒 Locked — Flutter MUST branch header visibility using ONLY integer comparison on `surah.id === 1 / === 9` (see DO/DON'T contract in the Guide). MUST NOT strip BOM or Bismillah on client. MUST NOT merge khatmah vs daily-journey counters. |
| P1 | Profile / my-qibla / journey / challenges / notifications for next Flutter UI pass | ⏳ QUEUED — Routes exist and return structured envelopes; wire up full-screen Flutter pages and deeper list interactions in the next client iteration. The dashboard payload already embeds summary tiles for all five. Full inventory of endpoints (40+) is audited in BACKEND_REQUIREMENTS.md §3/§4. | ⚠️ DO NOT DELETE — assume Flutter model classes already reference the current envelope/field names; prefer additive-only changes (new optional fields, new endpoints). |
